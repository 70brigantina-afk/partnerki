<section class="newsletter section">
  <div class="container">
    <div class="newsletter__box">
      <h2 class="section__title">Полезные подборки</h2>
      <p class="section__subtitle">
        Здесь в будущем появится рассылка с новыми программами и тематическими
        подборками. Пока раздел подготовлен как заглушка.
      </p>
      <p class="newsletter__note">
        Подписка будет доступна после запуска проекта.
      </p>
    </div>
  </div>
</section>

<style>
  .newsletter__box {
    padding: var(--space-8);
    background: var(--color-surface);
    border: 1px dashed var(--color-border);
    border-radius: var(--radius-xl);
    text-align: center;
  }
  .newsletter__note {
    margin: 0;
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }
</style>
